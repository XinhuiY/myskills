# Variant Not Found

Components or properties that couldn't be matched exactly during a Figma export. Per `modules/export-to-figma.md` "Process: when a variant cannot be found" — pick the closest available variant, log it here, and update `modules/export-to-figma.md` once the design system ships the missing variant.

| Element | Source location | Wanted | Closest picked | Notes |
|---|---|---|---|---|
| *(append rows here as you go)* | | | | |

## After the design system ships the missing variant

1. Re-export the affected screen.
2. Remove the corresponding row from this table.
3. Update `modules/export-to-figma.md` with the correct key so future exports don't re-hit the same gap.
